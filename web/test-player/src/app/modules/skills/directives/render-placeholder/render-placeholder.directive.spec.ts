import { RenderPlaceholderDirective } from './render-placeholder.directive';

describe('RenderPlaceholderDirective', () => {
  it('should create an instance', () => {
    const directive = null;
    expect(directive).toBeTruthy();
  });
});
