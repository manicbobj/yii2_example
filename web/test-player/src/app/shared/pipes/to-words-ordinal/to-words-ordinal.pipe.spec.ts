import { ToWordsOrdinalPipe } from './to-words-ordinal.pipe';

describe('ToWordsOrdinalPipe', () => {
  it('create an instance', () => {
    const pipe = new ToWordsOrdinalPipe();
    expect(pipe).toBeTruthy();
  });
});
