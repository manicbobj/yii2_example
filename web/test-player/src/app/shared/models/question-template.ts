export interface QuestionTemplateType {
  available_render_options: {},
  default_render_options: {},
  generator_engine: string,
  render_engine: string,
  scoring_engine: string,
  feedback_engine: string,
  template: string
}
